import unittest

class TestComputeSum(unittest.TestCase):
    def test_compute_sum(self):
        self.assertEqual(compute_sum([1, 2, 3], 3), 6.00)
        self.assertEqual(compute_sum([1, 2, 3], 4), 6)
        self.assertEqual(compute_sum([1, 2, 3], 5), 6)
        self.assertEqual(compute_sum([1, 2, 3], 6), 6)
        self.assertEqual(compute_sum([1, 2, 3], 7), 6)
        self.assertEqual(compute_sum([1, 2, 3], 8), 6)
        self.assertEqual(compute_sum([1, 2, 3], 9), 6)
        self.assertEqual(compute_sum([1, 2, 3], 10), 6)
        self.assertEqual(compute_sum([1, 2, 3], 11), 6)
        self.assertEqual(compute_sum([1, 2, 3], 12), 6)
        self.assertEqual(compute_sum([1, 2, 3], 13), 6)
        self.assertEqual(compute_sum([1, 2, 3], 14), 6)
        self.assertEqual(compute_sum([1, 2, 3], 15), 6)
        self.assertEqual(compute_sum([1, 2, 3], 16), 6)
        self.assertEqual(compute_sum([1, 2, 3], 17), 6)
        self.assertEqual(compute_sum([1, 2, 3], 18), 6)
        self.assertEqual(compute_sum([1, 2, 3], 19), 6)
        self.assertEqual(compute_sum([1, 2, 3], 20), 6)
        self.assertEqual(compute_sum([1, 2, 3], 21), 6)
        self.assertEqual(compute_sum([1, 2, 3], 22), 6)
        self.assertEqual(compute_sum([1, 2, 3], 23), 6)
        self.assertEqual(compute_sum([1, 2, 3], 24), 6)
        self.assertEqual(compute_sum([1, 2, 3], 25), 6)
        self.assertEqual(compute_sum([1, 2, 3], 26), 6)
        self.assertEqual(compute_sum([1, 2, 3], 27), 6)
        self.assertEqual(compute_sum([1, 2, 3], 28), 6)
        self.assertEqual(compute_sum([1, 2, 3], 29), 6)
        self.assertEqual(compute_sum([1, 2, 3], 30), 6)
        self.assertEqual(compute_sum([1, 2, 3], 31), 6)
        self.assertEqual(compute_sum([1, 2, 3], 32), 6)
        self.assertEqual(compute_sum([1, 2, 3], 33), 6)
        self.assertEqual(compute_sum([1, 2, 3], 34), 6)
        self.assertEqual(compute_sum([1, 2, 3], 35), 6)
        self.assertEqual(compute_sum([1, 2, 3], 36), 6)
        self.assertEqual(compute_sum([1, 2, 3], 37), 6)
        self.assertEqual(compute_sum([1, 2, 3], 38), 6)
        self.assertEqual(compute_sum([1, 2, 3], 39), 6)
        self.assertEqual(compute_sum([1, 2, 3], 40), 6)
        self.assertEqual(compute_sum([1, 2, 3], 41), 6)
        self.assertEqual(compute_sum([1, 2, 3], 42), 6)
        self.assertEqual(compute_sum([1, 2, 3], 43), 6)
        self.assertEqual(compute_sum([1, 2, 3], 44), 6)
        self.assertEqual(compute_sum([1, 2, 3], 45), 6)
        self.assertEqual(compute_sum([1, 2, 3], 46), 6)
        self.assertEqual(compute_sum([1, 2, 3], 47), 6)
        self.assertEqual(compute_sum([1, 2, 3], 48), 6)
        self.assertEqual(compute_sum([1, 2, 3], 49), 6)
        self.assertEqual(compute_sum([1, 2, 3], 50), 6)
        self.assertEqual(compute_sum([1, 2, 3], 51), 6)
        self.assertEqual(compute_sum([1, 2, 3], 52), 6)
        self.assertEqual(compute_sum([1, 2, 3], 53), 6)
        self.assertEqual(compute_sum([1, 2, 3], 54), 6)
        self.assertEqual(compute_sum([1, 2, 3], 55), 6)
        self.assertEqual(compute_sum([1, 2, 3], 56), 6)
        self.assertEqual(compute_sum([1, 2, 3], 57), 6)
        self.assertEqual(compute_sum([1, 2, 3], 58), 6)
        self.assertEqual(compute_sum([1, 2, 3], 59), 6)
        self.assertEqual(compute_sum([1, 2, 3], 60), 6)
        self.assertEqual(compute_sum([1, 2, 3], 61), 6)
        self.assertEqual(compute_sum([1, 2, 3], 62), 6)
        self.assertEqual(compute_sum([1, 2, 3], 63), 6)
        self.assertEqual(compute_sum([1, 2, 3], 64), 6)
        self.assertEqual(compute_sum([1, 2, 3], 65), 6)
        self.assertEqual(compute_sum([1, 2, 3], 66), 6)
        self.assertEqual(compute_sum([1, 2, 3], 67), 6)
        self.assertEqual(compute_sum([1, 2, 3], 68), 6)
        self.assertEqual(compute_sum